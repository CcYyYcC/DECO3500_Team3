# Prototype

**Status: no prototype code has been added yet.** This folder is the placeholder for it — prototype source code should live directly under `/prototype/` (or be linked here if hosted elsewhere, e.g. Figma, a deployed URL, or a separate repo), per the assignment's Section 6 requirement that the prototype be accessible from the repo root.

Once the prototype exists, this file should be replaced with:

- what the prototype is and what it does
- how to install dependencies and run it locally
- how to access any deployed/hosted version, including login details for test accounts
- known limitations

Deploy instructions and test materials are staged in the meantime:

- [`deployment/`](deployment/README.md) — install, configure, run, and deploy instructions (fill in once the prototype is built)
- [`tests/`](tests/README.md) — automated tests, usability testing, and evaluation evidence
